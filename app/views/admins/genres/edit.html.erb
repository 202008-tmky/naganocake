<h1>ジャンル編集</h1>

<%= form_for @genre, url: admins_genre_path do |f| %>
	<h4>ジャンル</h4>
	<%= f.text_field :name %>
	<%= f.select :is_active ,[['有効',true],['無効',false]] %>
	<%= f.submit '変更を保存する' %>
<% end %>