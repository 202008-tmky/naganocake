<div class="item-index-box">

    <!-- ジャンル選択のサイドバー -->
    <%= render "items/genres_sidebar", genres: @genres %>
    <!-- ジャンル選択のサイドバー -->
    <div class="item-index">
    <h3>商品一覧</h3>
        <table>
          <tbody>
            <tr class="item-box">
              <% @genres.items.each do |item| %>
              <% if @genres.is_active == true && @genres.items.is_active == true %>
                <td class="item">
                    <%= link_to item_path(item) do %>
                      <%= attachment_image_tag(item, :image, :fill, 300, 300, fallback:"top.image.jpg") %><br>
            	      <%= item.name %><br>
            	      ￥<%= item.price %>
                    <% end %>
                </td>
              <% end %>
              <% end %>
            </tr>
          </tbody>
        </table>
    </div>
</div>